{
  "superpowers-subagent-driven-development": {
    "allowed_work assignment": ["Serial subagent-driven"],
    "allowed_mode": ["serial-single-active"],
    "max_concurrent": "1",
    "required_controls": [
      "ordered work package sequence",
      "integration check after each package"
    ]
  },
  "superpowers-dispatching-parallel-agents": {
    "allowed_work assignment": ["Parallel subagent-driven"],
    "allowed_mode": ["parallel-max-safe"],
    "max_concurrent": "auto or N",
    "required_controls": [
      "spine frontier",
      "parallel wave matrix",
      "conflict / lock model",
      "failure policy",
      "main-agent integration rule"
    ]
  },
  "bounded-protocol": {
    "allowed_work assignment": ["Serial subagent-driven", "Parallel subagent-driven"],
    "allowed_mode": ["serial-single-active", "parallel-max-safe"],
    "max_concurrent": "1 / auto / N",
    "required_controls": [
      "cybernetic execution policy owns safety"
    ]
  },
  "adapter-specific": {
    "allowed_work assignment": ["Serial subagent-driven", "Parallel subagent-driven"],
    "allowed_mode": ["serial-single-active", "parallel-max-safe"],
    "max_concurrent": "registry-defined",
    "required_controls": [
      "adapter compatibility rationale",
      "cybernetic execution policy owns safety"
    ]
  },
  "none": {
    "allowed_work assignment": ["Main-only"],
    "allowed_mode": ["none"],
    "max_concurrent": "0",
    "required_controls": [
      "main-only context-load rationale when Level 3/4"
    ]
  }
}
