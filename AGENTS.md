# Agent Instructions

- After every completed execution, run the appropriate verification, commit the resulting changes, and push to the configured remote.
- If the user explicitly says not to commit or not to push, follow the latest user instruction.
